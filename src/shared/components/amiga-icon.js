// Chat uses the committed public copy of the Android launcher icon.
// Keep this as a plain public URL so `scripts/extract-android-icon.cjs`
// remains the single re-sync path after Android icon changes.
export const amigaIconUrl = "/amiga-icon.png";
